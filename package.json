{
  "name": "@sniptt-official/ots-aws",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!node_modules"
  ],
  "scripts": {
    "postinstall": "tsc",
    "format": "prettier --write '{.github,lambda-functions,lib,test}/**' '*.json'",
    "format:check": "prettier --check '{.github,lambda-functions,lib,test}/**' '*.json'",
    "build": "tsc",
    "build:lambda-functions": "cd lib/constructs/lambda/functions; tsc",
    "watch": "tsc -w",
    "test": "jest"
  },
  "prettier": "@slavovojacek/prettier-config",
  "devDependencies": {
    "@slavovojacek/prettier-config": "^0",
    "@slavovojacek/tsconfig": "^0",
    "@types/jest": "^27",
    "@types/node": "^17",
    "aws-cdk-lib": "^2.16",
    "constructs": "^10.0",
    "jest": "^27",
    "ts-jest": "^27",
    "typescript": "^4.6"
  },
  "peerDependencies": {
    "aws-cdk-lib": "^2.16",
    "constructs": "^10.0"
  },
  "dependencies": {
    "env-var": "^7.1"
  }
}
